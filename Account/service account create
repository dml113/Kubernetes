# policy는 원하는 권한으로 미리 생성한다.

# service account 생성
cluster=skills-cluster
policy=policy arn 입력

# account 생성
eksctl create iamserviceaccount --cluster $cluster --attach-policy-arn $policy --namespace default --name secret-role --approve

# service account 생성됐는지 확인
kubectl get sa
