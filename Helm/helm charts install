sudo yum install -y git
helm plugin install https://github.com/hypnoglow/helm-s3.git
helm s3 init s3://<bucket name>/charts
helm repo add my-helm-charts s3://<bucket name>/charts

# tgz file Create
helm package ./my-nginx/
helm s3 push ./my-nginx-0.1.0.tgz my-helm-charts
helm upgrade my-nginx-release my-helm-charts/my-nginx --version 0.1.0 --namespace helm-space
