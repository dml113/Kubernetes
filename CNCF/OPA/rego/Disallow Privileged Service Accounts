package k8sprivilegedserviceaccounts

violation[{"msg": msg}] {
  serviceAccount := input.review.object.spec.serviceAccountName
  serviceAccount == "root"
  msg := "Service account 'root' is not allowed."
}
